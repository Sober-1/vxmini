<view class="tab-control">
  <block wx:for="{{list}}" wx:key="{{index}}">
        <view class="tab-item {{currentIndex===index ? 'active' : ''}}" bind:tap="itemClick" data-index="{{index}}">
    <text>{{item}}</text>
    </view>
  </block>
</view>